/*
*  Writing to a file via UART
*/

#include <stdio.h>
#include <stdlib.h>
#include <file.h>
#include <uart.h> 

void file_write ()
{
	//FILE *fp;
	
	//fp = fopen(filename, "w");
	
	//if (fp == NULL)
	//{
	//	perror("Error opening file: ");
	//	exit(1);
	//}
	
	//fprintf(fp, "%s", filemsg);
	//fclose(fp);
}